/*fill the following variables with values according to their names*/

var undefined_var;
var string_var;
var string2_var;
var number_var;
var number2_var;
/***************************************/

//using the variables above, create new variables that are the following
/*
1) concatenation of 2 strings
2) concatenation of a string and an empty variable
3) addition of 2 numbers
4) concatenation of a string, a number, and an empty variable
5) addition of 2 strings and a number
6) concatenation of a string with the addition of 2 numbers
7) assigning a string variable to a new var (copying)
8) concatenation of 2 numbers as strings
/***************************************/


/*define a function that takes 2 parameters, num1 and num2, and multiplies them together.  Have the function return the result*/


/***************************************/
/*define a function that takes 3 parameters
num1, num2, num3
If num3 is not provided (undefined), it sets the value to 0
It then multiples num2 by num1, and then subtracts num3.
return the result
*/

/***************************************/
/*create a function that 
1) creates a new empty array.
2) iterates through the following array*/

var test_vars = [5, 15, 28, 105, 22, -7, 75, 99];

/*
3) inside the for loop, create a conditional that accomplishes the following


//Less than 25 - adds the string 'one quarter' to the new array
//greater than 75 - adds the string '3 quarters' to the new array
//Less than zero or greater than 100 - adds the string "invalid" to the new array
//25 or greater and 75 and less, adds the string 'median' to the new array

// have the function return the new array

/***************************************/

